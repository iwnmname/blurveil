[project]
name = "blurveil"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mss>=10.1.0; sys_platform != 'linux'",
    "opencv-python>=4.12.0.88; sys_platform != 'linux'",
    "pillow>=12.0.0; sys_platform != 'linux'",
    "pynput>=1.8.1; sys_platform != 'linux'",
    "pyqt6>=6.10.0; sys_platform != 'linux'",
    "pytesseract>=0.3.13; sys_platform != 'linux'",
]
